using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/// <summary>
/// this class is not used but might be used for debugging purposes
/// </summary>
public class SkillButton : MonoBehaviour {

    //public int id;
	public void executeSkill()
	{
		//GameObject.Find("Turn-basedSystem").GetComponent<turnSystemScript09>().CastSpellOnTarget(id);
	}

	public void showSkillInfo()
	{
	//	string skillInfo = skillToPerform.actionCost.ToString() + " MP: " + skillToPerform.actionDesc;
	//	GameObject.Find("Turn-basedSystem").GetComponent<turnSystemScript09>().activateSkillDescPanel(skillInfo);
	}

	public void hideSkillInfo()
	{
	//	GameObject.Find("Turn-basedSystem").GetComponent<turnSystemScript09>().deactivateSkillDescPanel();
	}
}
